/* eslint-disable no-restricted-syntax */
const styles = {
  scrollProgress: {
    position: 'fixed',
    top: 0,
    left: 0,
    right: 0,
    height: '3px',
    transformOrigin: '0%',
  },
};

export default styles;
